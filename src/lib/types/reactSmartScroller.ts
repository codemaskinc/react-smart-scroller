import React from 'react'

export type ReactSmartSliderProps = {
    numCols?: number,
    spacing?: number,
    trackProps?: React.CSSProperties,
    thumb?: JSX.Element,
    vertical?: boolean,
    draggable?: boolean
}
